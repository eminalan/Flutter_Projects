import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF1565C0); // Mavi tonları (Güven verici)
  static const Color secondary = Color(0xFF42A5F5); // Açık mavi (Canlılık)
  static const Color background = Color(0xFFF5F5F5); // Açık gri (Göz yormayan)
  static const Color textPrimary = Color(0xFF212121); // Siyah (Okunabilirlik)
  static const Color textSecondary = Color(0xFF757575); // Gri (Daha az vurgu)
  static const Color accent = Color(0xFFFFA726); // Turuncu (Dikkat çekici)
}
class AppColorsDark {
  static const Color primary = Color(0xFF0D47A1); // Koyu mavi (Güven verici)
  static const Color secondary = Color(0xFF1976D2); // Orta mavi (Canlılık)
  static const Color background = Color(0xFF121212); // Koyu gri (Göz yormayan)
  static const Color textPrimary = Color(0xFFFFFFFF); // Beyaz (Okunabilirlik)
  static const Color textSecondary = Color(0xFFBDBDBD); // Açık gri (Daha az vurgu)
  static const Color accent = Color(0xFFFF7043); // Turuncu (Dikkat çekici)
}
class AppColorsLight {
  static const Color primary = Color(0xFF1565C0); // Mavi tonları (Güven verici)
  static const Color secondary = Color(0xFF42A5F5); // Açık mavi (Canlılık)
  static const Color background = Color(0xFFF5F5F5); // Açık gri (Göz yormayan)
  static const Color textPrimary = Color(0xFF212121); // Siyah (Okunabilirlik)
  static const Color textSecondary = Color(0xFF757575); // Gri (Daha az vurgu)
  static const Color accent = Color(0xFFFFA726); // Turuncu (Dikkat çekici)
}

