buildscript {
    repositories {
        google() // Google Maven deposunu ekle
        mavenCentral() // Maven merkezi deposunu ekle
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.1.2'  // Gradle sürümünü kontrol et
        classpath 'com.google.gms:google-services:4.3.8' // Google Services
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
